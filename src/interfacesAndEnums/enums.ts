// global elements
export enum ETargetLink {
  BLANK = '_blank',
  SELF = '_self',
}
export enum EButtonType {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
  LINK = 'LINK',
}

export enum ERel {
  NOFOLLOW = 'nofollow',
}
// end global elements
