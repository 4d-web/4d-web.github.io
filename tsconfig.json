{
  "compilerOptions": {
    "module": "commonjs",
    "rootDir": "./src", // ресурси
    "outDir": "./build", // куди збирати
   "plugins": [{ "name": "typescript-plugin-css-modules" }],
    "resolveJsonModule": true, // завантаження json (оскільки вони досить важкі їх блокують за замовчуваням)
    "target": "ES6",
    "lib": ["DOM", "ES6", "DOM.Iterable", "ScriptHost", "ES2016.Array.Include"],
    "jsx": "react-jsx",
    "declaration": true, /* Генерує відповідний файл '.d.ts'. */
    "declarationMap": true, /* Генерує вихідну карту для кожного відповідного файлу '.d.ts'. */
    "sourceMap": true, /* Генерує відповідний файл '.map'. */
    "alwaysStrict": true, /* Розбирати в строгому режимі та видавати "use strict" для кожного вихідного файлу. */
    "allowSyntheticDefaultImports": true, /* Дозволити імпорт за замовчуванням з модулів без експорту за замовчуванням. Це не впливає на видачу коду, лише перевірку типу. */
    "esModuleInterop": true,
    // "allowJs": true, /* Дозволяє компілювати файли javascript. */
    // "checkJs": правда, /* Повідомити про помилки у файлах .js. */
    // "jsx": "preserve", /* Вкажіть генерацію коду JSX: 'preserve', 'react-native' або 'react'. */
    // "outFile": "./", /* Об'єднати та передати вихід в один файл. */
    // "outDir": "./dist", /* Перенаправити структуру виводу до каталогу. */
    // "rootDir": "./", /* Вкажіть кореневий каталог вхідних файлів. Використовуйте для керування структурою вихідного каталогу за допомогою --outDir. */
    // "composite": true, /* Увімкнути компіляцію проекту */
    // "removeComments": true, /* Не надсилати коментарі для виведення. */
    // "noEmit": true, /* Не видавати вихідні дані. */
    // "importHelpers": true, /* Імпорт помічників емітування з 'tslib'. */
    // "downlevelIteration": true, /* Забезпечує повну підтримку ітеративних елементів у 'for-of', розповсюдження та деструктурування, коли націлено на 'ES5' або 'ES3'. */
    // "isolatedModules": true, /* Транспілюємо кожен файл як окремий модуль (подібно до 'ts.transpileModule'). */
    /* Суворі параметри перевірки типу */
    //"strict": true, /* Увімкнути всі параметри суворої перевірки типів. */
    // "noImplicitAny": true, /* Викликає помилку у виразах та оголошеннях із неявним типом 'any'. */
    // "strictNullChecks": true, /* Увімкнути строгі перевірки на нуль. */
    // "strictFunctionTypes": true, /* Увімкнути сувору перевірку типів функцій. */
    // "strictPropertyInitialization": true, /* Увімкнути сувору перевірку ініціалізації властивості в класах. */
    // "noImplicitThis": true, /* Викликати помилку у виразах 'this' з неявним типом 'any'. */
    /* Додаткові перевірки */
    // "noUnusedLocals": true, /* Повідомити про помилки на невикористаних локальних параметрах. */
    // "noUnusedParameters": true, /* Повідомити про помилки щодо невикористаних параметрів. */
    // "noImplicitReturns": true, /* Повідомити про помилку, коли не всі шляхи коду у функції повертають значення. */
    // "noFallthroughCasesInSwitch": true, /* Повідомити про помилки для випадків пропуску в операторі switch. */
    /* Параметри роздільної здатності модуля */
    // "moduleResolution": "node", /* Вкажіть стратегію розділення модуля: 'node' (Node.js) або 'classic' (TypeScript до 1.6). */
    // "baseUrl": "./", /* Базовий каталог для вирішення неабсолютних імен модулів. */
    // "шляхи": {}, /* Серія записів, які переназначають імпорт на місця пошуку відносно 'baseUrl'. */
    // "rootDirs": [], /* Список кореневих папок, чий об'єднаний вміст представляє структуру проекту під час виконання. */
    // "typeRoots": [ /* Список папок для включення визначення типів. */
    //    "modules_node/@types",
    //    "src/typings.d.ts"
    // ],
    // "типи": [], /* Файли оголошення типів, які будуть включені в компіляцію. */
    /* Дозволяє випромінювати взаємодію між модулями CommonJS і ES шляхом створення об'єктів простору імен для всіх імпортів. Має на увазі 'allowSyntheticDefaultImports'. */
    // "preserveSymlinks": true, /* Не розв'язувати справжній шлях символічних посилань. */
    /* Параметри карти джерела */
    // "sourceRoot": "", /* Вкажіть місце розташування, де налагоджувач повинен знаходити файли TypeScript замість джерел. */
    // "mapRoot": "", /* Вкажіть розташування, де налагоджувач повинен знаходити файли карти замість згенерованих місць. */
    // "inlineSourceMap": true, /* Випускати один файл із вихідними картами замість окремого файлу. */
    /* Основні параметри */
    // "target": "es5", /* Вкажіть цільову версію ECMAScript: 'ES3' (за замовчуванням), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018' або 'ESNEXT'. */
    // "module": "commonjs", /* Вкажіть генерацію коду модуля: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015' або 'ESNext'. */
    // "lib": [ /* Вкажіть файли бібліотеки, які будуть включені до компіляції. */
    //    "далі",
    //    "дом"
    // ],
  },
  //  "include": ["src/**/*"],
  //  "exclude": ["node_modules", "**/*.spec.ts"]
  "include": [
    "src",
    "stories",
    "declaration.d.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}